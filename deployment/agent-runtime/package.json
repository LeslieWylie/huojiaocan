{
  "name": "huojiaocan-agent-runtime",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "migration:generate": "node scripts/generate-migration.mjs",
    "check": "node --check src/index.js && node --check src/runtime.js && node --check src/request-store.js && npm run test && node scripts/check-migration.mjs",
    "build": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "@openmaic/storage": "0.29.0",
    "pg": "8.16.3"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.3.14",
    "wrangler": "4.129.0"
  }
}
