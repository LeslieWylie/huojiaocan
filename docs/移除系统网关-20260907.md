# 移除系统网关

## 已生效的生产变更
- 删除Production的LLM_GATEWAY_API_KEY、LLM_GATEWAY_BASE_URL、LLM_GATEWAY_MODEL、LLM_GATEWAY_TIMEOUT_MS、LLM_GATEWAY_MAX_TOKENS。
- 重新部署原生产提交7d20576，不捆绑开发分支的其他变更。
- 新部署live-teacher-guide-nw5tczv05-roy-leos-projects.vercel.app，固定地址保持不变。
- 固定地址/api/config验证：gatewayConfigured=false、textModelConfigured=false、imageModelConfigured=false。
- 未删除、修改个人DeepSeek密钥、密钥加密配置、Supabase或教材索引配置。
- 删除Vercel变量不会撤销网关供应商端密钥；未修改其他项目或供应商账户。

## 开发分支代码变更（尚未发布）
- 问答、三卡、重新生成、教学取舍和作业分析均解析当前账号个人密钥；省略keyId使用该账号默认连接，无可用连接返回错误，不回退系统网关。
- 问答选择器不再提供系统智能选项；设置页不再引导使用系统网关。
- 顶栏检查个人连接配置，不宣称网关可用。
- 公开模型目录只列两种DeepSeek模型，不访问网关；旧图片生成接口返回停用提示。
- 保留内部网关适配器供历史测试/离线比较兼容，但教师API不再通过其选择系统连接。

## 验证
- 705项单测通过，静态检查及构建通过。
- 登录交接与恢复浏览器套件16项通过，夹具改为显式个人连接。
- 最后补充“没有个人密钥不得调用provider”的断言，账号隔离套件7项通过。
- 本次生产仅验证配置删除生效；不把开发分支测试说成其代码已在线上运行。

## 发布收尾
本节取代上文“代码尚未发布”的状态。
- 基于原生产提交单独创建 yaole/remove-system-gateway-release，不包含教学技能和持久执行等开发分支改动。
- 发布应用变更来自 75fcbbc、1fef020；4e76e74 清除误纳入的本机依赖链接。
- 已将 live-teacher-guide-amdkc2dh8-roy-leos-projects.vercel.app 提升至生产。
- 固定地址真实浏览器复查：个人连接选项1个、系统选项0个、gatewayConfigured=false；模型目录仅列 deepseek-v4-flash/pro，未观察到页面脚本异常。
- 修复同一教材问答URL刷新时未发送输入被丢弃：仅允许同账号、同页面恢复，不给恢复文字附加自动发送权限。
- 发布分支666项单测、静态检查和构建通过；个人连接主流程1项完整E2E和会话恢复2项E2E通过。
- 主流程使用本地加密个人密钥及确定性模型测试传输，覆盖连续提问、三卡、保存、锁定和课堂使用，不将夹具结果描述为真实付费模型验证。
- 本轮公网只读检查，不创建真实教师草稿，未进行新的真实付费模型调用。第二批持久执行未发布。
