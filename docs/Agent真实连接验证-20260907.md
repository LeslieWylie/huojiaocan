# Agent真实连接验证与后续修复

## 执行范围
读取Vercel当前生产配置，用本地开发代码进行只读索引调用与最小模型探测。
未修改生产变量、未创建教师草稿、未迁移数据库、未部署。
临时配置在仓库外以限制权限保存，测试后删除。不记录密钥、认证头或完整环境配置。

## 实际观察
1. 岳阳楼记原文核对案例：约29.4秒，返回blocked-no-evidence；未取得合格原页，未生成结论。记录到1次模型端点请求尝试，检索执行摘要为model_error。
2. 独立目录探测：学生教材、教师用书均约10.5秒后pageindex_request_failed。此结果限于本机到配置服务的网络路径，不代表已证明生产函数内同样失败。
3. 独立最小JSON模型探测：1次请求，HTTP401，gateway_unauthorized；没有自动重试。读取的密钥值不是掩码占位符，但当前配置连接未通过认证。
4. 因首个案例阻断，未批量运行剩余付费案例，没有宣称12例质量对照完成。
5. Ego Lite工作区报告用户接管，已停止浏览器控制；没有绕过接管状态或另开空间替代。

## 本次修复
- 中文引文后的“写的是谁/描写对象/比较/含义”触发原页阅读，无须教师额外输入“核对原文”。
- 第二轮审校通过且没有待解决问题时立即结束，不因可选第三轮时间不足错误降级。
- 新增两个回归测试。全量705项单测通过，静态检查与构建通过。

## 尚未满足的发布条件
- 恢复有效模型连接后重做最小探测，再跑12例对照；不通过修改提示词掩盖认证错误。
- 分别核对本机与预览部署到教材索引的连接，区分本机网络问题和服务不可用。
- 浏览器控制恢复后完成真实账号主流程。
- 本轮不部署。此前自动化浏览器测试属于夹具验证，不代替上述真实链路。

## 继续核验：两条连接不可混为一谈
- 生产系统网关配置的地址为 llm-gateway.mlamp.cn，模型为 tencent/deepseek-v4-pro；路径规范化、前后空白和重复Bearer前缀未发现问题。
- 只读模型目录接口返回401，响应明确属于凭据无效而非余额不足。未替换系统凭据。
- 在Ego Lite现有登录态中，当前个人DeepSeek连接末四位2ad1；线上测试接口返回200。
- 随后明确使用该个人连接提交一次岳阳楼记原文核对：问答200、grounded-deepseek、8条引用、草稿创建201。
- 回答区分“迁客骚人”与“古仁人之心”；保存后重新打开草稿，回答与篇目恢复，无页面错误提示。
- 独立测试草稿标识 afcaad7b-ac73-40fc-b410-d25d3cb85396，班级标记“回归0907-连接”；不修改既有教师草稿。
- 引用包括学生教材57/58页、教师用书225/231/232页等。部分附带引用较宽泛，不能据此宣称所有引用质量完全通过。
- 本次是真实生产旧版本的连接和保存验证，不代表尚未部署的新分支已经完成12例对照。

### 修正误导性状态
页面此前把“配置存在”显示成“系统AI可用”，未实际测试认证。
改成“系统连接已配置”，并说明不代表连接已测试成功；401提示不再建议原地反复重试。
静态检查与构建通过；新增浏览器用例验证配置状态不冒充连通性，1项通过。
- 清理仅针对本轮测试ID，并先核对班级标记及版本1。DELETE返回412，但随后的同账号GET返回404；未重试删除。记录这一状态不一致，后续需核对生产删除处理，不能写成删除接口正常200。
- 已关闭本次新增的3个测试标签页，保留用户原先的单元接力页。临时生产配置已删除。
